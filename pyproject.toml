[project]
name = "hello-agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ricardo-Ping" }
]
requires-python = ">=3.10,<3.11"
dependencies = [
    "dotenv>=0.9.9",
    "openai>=2.52.0",
    "requests>=2.34.2",
    "tavily>=1.1.0",
    "torch>=2.13.0",
    "transformers>=5.14.1",
]

[project.scripts]
hello-agents = "hello_agents:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
